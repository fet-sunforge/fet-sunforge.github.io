import 'geist/dist/fonts/geist-sans/style.css' // Imports the Geist Sans font faces
import 'geist/dist/fonts/geist-mono/style.css' // Imports the Geist Mono font faces

import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')
